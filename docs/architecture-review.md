# Architecture review — findings and resolution trace

Status: trace of the two adversarial review passes over the architecture set (`docs/architecture.md`, `docs/research/mcp-contracts.md`, `tasks/prd-kiwifs-memory.md`). Prior findings F1–F10 and R1–R5 came from the first review (workflow run `kiwifs-mcp-architecture-mtsrn0qs-1oe9qz`, field `result.review`); B-1…M-3 come from the second (source-verified) review. Resolution state below is verified against the current documents and against local Pi 0.85.0 source (`node_modules/@earendil-works/pi-coding-agent/dist/core/agent-session.js`, `…/types.d.ts`) rather than accepted from the reviewer unchecked.

## Second review (current) — resolution

| ID  | Severity    | Finding                                                                                                                                                                                    | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B-1 | Blocking    | §3.1 fingerprint spec self-defeating: redacted text vs unredacted stored messages; input text observed pre-expansion but queued messages replay expanded; "input event position" undefined | Fixed in `architecture.md` §3.1 and `mcp-contracts.md` §6: fingerprint = hash of **raw unredacted input-event text + `streamingBehavior`** (local matching state, never outbound); all `/`-prefixed inputs ineligible so expansion cannot diverge eligible fingerprints; "position" dropped. Verified in source: `emitInput` pre-expansion (`agent-session.js:841–853`), queued steer/followUp carry `expandedText` (`:853–868`). Fixtures extended: T13 and `mcp-contracts.md` §9 fixture 11 now require a redaction-active variant and a template-expanded queued variant |
| B-2 | Blocking    | PRD listed the 3,000-token evidence budget among unapproved proposals, re-opening confirmed decisions.md #7                                                                                | Fixed in PRD "Research constraints": struck from the proposal list and explicitly marked confirmed (decisions.md #7)                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| S-1 | Significant | PRD "Proposed architecture" section still said "verified REST/MCP capabilities… native `fetch` sufficient" — contradicts confirmed MCP-only                                                | Section marked superseded with an explicit note; the REST sentence struck and replaced with the confirmed MCP-only disposition (decisions.md #1)                                                                                                                                                                                                                                                                                                                                                                                                                            |
| S-2 | Significant | Extraction model mislabeled [P] in architecture §1/§2 despite decisions.md #9 confirming it                                                                                                | Fixed: label now "OpenRouter z-ai/glm-5.3-flash, configurable (confirmed, decisions.md #9)" in the component diagram and Observer table row                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| S-3 | Significant | B1 (endpoint auth hard prerequisite) unreconciled with T04/T19 live runner against port 8182 whose auth enforcement is unverified                                                          | Reconciliation sentence added to `architecture.md` §9 and PRD T04: B1 governs the production endpoint; the live runner targets only the network-restricted dedicated test service; 8182 auth behavior is recorded as evidence, never assumed (consistent with `test-environment.md`: authenticated success proves connectivity, not enforcement)                                                                                                                                                                                                                            |
| M-1 | Minor       | Board GC clause "acknowledged by all known local consumers" unverifiable — acknowledgments are per-agent local state with no shared record                                                 | Softened in `architecture.md` §8: "acknowledged locally by this agent"; accepted limitation documented (an agent offline beyond the grace window may lose a message); grace period + manual confirmation are the compensating controls. §13 row 13 default unchanged                                                                                                                                                                                                                                                                                                        |
| M-2 | Minor       | "61 tool names" overstated precision — the source list contains wildcards                                                                                                                  | Fixed in `mcp-contracts.md` §3: "61 tool name patterns"; noted that 61 counts patterns, not concrete tools. Live `tools/list` (71) remains authoritative                                                                                                                                                                                                                                                                                                                                                                                                                    |
| M-3 | Minor       | followUp run boundary unstated: a followUp may begin a new agent run with no `before_agent_start` either                                                                                   | Verified in source (`emitBeforeAgentStart` only inside `prompt()`, `agent-session.js:915`) and stated in `architecture.md` §3.1, `mcp-contracts.md` §6, and PRD T13                                                                                                                                                                                                                                                                                                                                                                                                         |

## First review — final status

| ID  | Status   | Notes                                                                                                                                                                                                                                               |
| --- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1  | Resolved | Match-must-inject mechanism verified feasible (`context` fires per provider call with full history; fresh turns via `before_agent_start`). Was partial due to broken fingerprint spec — completed by B-1 fix above; T13 fixture is the gating proof |
| F2  | Resolved | Scope-critical legs restricted to `kiwi_search`/`kiwi_search_semantic`; brief pinned-prefix + read-back post-filter + rebuild (§3.1, fixture 5a)                                                                                                    |
| F3  | Resolved | Explicit 5-step fail-closed guard predicate (§3.1, `mcp-contracts.md` §8, fixture 5c)                                                                                                                                                               |
| F4  | Resolved | `msg_id` derived from persisted `opId` (`channel + from + opId`), never content hash (§2, §8, fixture 5b)                                                                                                                                           |
| F5  | Resolved | Record `scope` is a single owner value; session holds an authorized scope set; fanout N ≤ 4 (§2, §3.1)                                                                                                                                              |
| F6  | Resolved | Durable `opId` persisted before any side effect; T09 acceptance criterion enforces it (§2, §3.2)                                                                                                                                                    |
| F7  | Resolved | Tombstone cache scoped to the authorized scope set, advisory only; a cache miss never permits injection (§3.1)                                                                                                                                      |
| F8  | Resolved | Manual-only board GC with confirmation and grace window; the residual "acked by all consumers" clause was fixed under M-1                                                                                                                           |
| F9  | Resolved | `result.message` singular confirmed (`types.d.ts:845–849`); merge rule stated in §3.1/T13                                                                                                                                                           |
| F10 | Resolved | Extraction model confirmed (decisions.md #9); the relabeling regression (S-2) is fixed. The related B-2 relisting regression is also fixed                                                                                                          |
| R1  | Resolved | Chunk boundaries decided by the durable coverage cursor, not the triggering lifecycle event (§7)                                                                                                                                                    |
| R2  | Resolved | No backend tombstone path; tombstones are local cache state only (§2)                                                                                                                                                                               |
| R3  | Resolved | Fixture 11 asserts matched injection on the consuming provider call, not merely the next `context` fire                                                                                                                                             |
| R4  | Resolved | Reconciliation bounded ≤20 pages / 10,000 changes per pass, visible pause (§6, §13 row 14)                                                                                                                                                          |
| R5  | Resolved | `kiwi_append` no longer used for immutable records; dual-sender collision guarded by op-id-derived paths (§4, §8, fixture 5b)                                                                                                                       |

## Remaining gates (not documentation findings)

1. **User approval of the architecture** (T02 final checkbox): architecture, §13 default table, and this review's resolutions require explicit user sign-off. Rows #18–#20 do not block v1: automatic remote deletion and restore-into-Pi are deferred; unredacted archival is excluded by confirmed privacy requirements. Final coordinator corrections also replace drop-oldest outbox overflow with capture backpressure that preserves pending work, and require model-compatible token accounting rather than a characters÷4 estimate. T07 and T12/T13 acceptance criteria cover these corrections.
2. **T13 feasibility fixture** must pass before T12/T13 land; if the consumed-input linkage fails in practice, steer/followUp retrieval degrades to opt-in with visible status (§3.1 disposition).
3. **B1 production-endpoint auth** must be in place before the extension is enabled outside the dedicated test space.
4. **Live runner (T04/T19)** remains unbuilt; all current live evidence comes from the one-time provisioning checks in `docs/test-environment.md`.
