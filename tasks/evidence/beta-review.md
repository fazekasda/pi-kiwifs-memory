# Beta review-and-repair record (R01, attempt 1)

Repairs applied in response to the four independent reviews. No commits made.

## Blockers fixed

1. **`tasks/evidence/t19-budget-report.json` revert (B01).** Working-tree diff
   was pure timing jitter (`latency50msPerCall.runs`: 964→979, 959→962, an
   extra 961 sample). Reverted with `git checkout --`. Re-generated by
   `npm run check` twice during verification; reverted again each time
   (confirmed jitter-only before each revert). `docs/research/beta-readiness.md`
   §5 corrected to quote the actual diff (was "964→968, 959→958").
2. **Stale test count.** Suite re-measured twice: **715 pass / 0 fail**.
   Updated 680→715 in `docs/research/beta-readiness.md` (§1 row, end note),
   `README.md` (test-suite line), `docs/quality-review.md` (annotated: 680 at
   Q10D, 715 after B03–B06 additions). Decision: counts state "re-measured;
   pinned to the candidate SHA at B09" rather than pretending permanence.
   Also rewrote beta-readiness §1 "Stale references found (not edited)" —
   both items are now corrected in-tree — to "Corrected in this tree".
3. **`npm run check` prettier failures.** Ran `npx prettier --write .` over
   the 13 flagged files (issue templates, SECURITY.md, beta-acceptance,
   beta-eval fixtures, model-eval summaries, beta-readiness). Formatting
   only; no content changes. `npm run check` now exits 0.
4. **OpenRouter model slug (paid-run blocker, both harnesses).** Split into
   `OPENROUTER_MODEL_ID = "z-ai/glm-5.3-flash"` (sent in request bodies) and
   `MODEL_SLUG = "openrouter/z-ai/glm-5.3-flash"` (recorded route in reports,
   unchanged for tests). Files: `scripts/tokenizer-provider-compare.mjs`,
   `scripts/model-eval.mjs`, both `.d.mts`.
5. **F1 — runner-version doc comment.** `src/backend/live/runner.ts` comment
   claimed the default is read from package.json; it is a pinned literal.
   Fixed the comment to state the pin and the obligation to bump it with any
   package.json version change (the alternative — reading package.json at
   runtime — was not chosen to keep the change minimal and review-owned).
6. **F2 — privacy-doc figure.** `docs/beta-privacy.md` "≤20 pages / 10,000
   changes" corrected to the code truth: ≤20 pages of ≤200 raw rows, ≤1,000
   kept board paths per cycle (`src/board/delivery.ts` MAX_LISTING_*).
7. **Hygiene — `.gitignore`.** Added `tasks/evidence/model-eval/` so B05 run
   output cannot be committed into the B09 candidate.

## Blockers NOT fixed (owner/decision required — documented, not speculative-fixed)

- **B05 matcher validity + fabrication/scope-leakage fusion + pre-parse
  wrong-scope scoring + canary surface coverage.** These change threshold
  semantics, and the plan requires thresholds frozen _before_ the paid run.
  Changing them silently would weaken the frozen rubric. Deferred to the B05
  owning task / user decision before any paid run.
- **B05 torn-last-line resume robustness and stale `0af731b3…` run dir** —
  owning-task follow-ups (removal decision belongs to B05/B09).
- **B04a framing-offset note (reflection re-rendering)** — doc-only follow-up.
- **F3 — B07 human TUI sign-off** cannot be produced by a repair pass; it is
  an execution task.

## Verification actually run

- `npm test` (twice): 715 pass / 0 fail.
- `node --test test/tokenizer-provider-compare.test.ts test/model-eval.test.ts
test/live-runner.test.ts test/q06c1-status.test.ts`: 42 pass / 0 fail.
- `npm run check`: **exit 0** (typecheck + prettier + 715 tests).
- `git diff --check`: clean.
- `npx prettier --check` on all repaired files: clean.
- `git check-ignore tasks/evidence/model-eval/`: ignored.

## Review-conflict decisions

- Test count: first review said suite is 715 while report said 680 — resolved
  by re-measurement (715) with a pin-at-B09 note instead of a bare number.
- Runner version (F1): reviews offered runtime-read vs comment fix; chose the
  comment fix (minimal, and a runtime read would change B06 behavior).

---

# R02, attempt 1 — resolution of final-review follow-ups

Both final reviews (re-review of full beta-prep diff B01–B09; test-correctness
and determinism re-review) returned **zero blockers**. The actionable items
were three non-blocking follow-ups. All three are now resolved in-tree;
no commits made.

## Finding-to-fix mapping

1. **`DEFAULT_RUNNER_VERSION` bump dependency (review 1, follow-up 1).**
   The in-code comment already states the obligation (fixed in R01, F1).
   Now also an explicit B09 acceptance-criteria item in
   `tasks/beta-release-plan.md` (bump in the same commit as the package
   version bump). **Fixed.**
2. **`docs/beta-privacy.md` "Release status" caveat re-check at B09**
   (review 1, follow-up 2). Verified present and accurate for the current
   stage: it disclaims that B04–B07 validation tasks have not passed yet
   (§148–153). Added a B09 acceptance-criteria re-check item so the caveat
   is revisited when evidence lands. **Fixed (via plan checklist).**
3. **`tasks/evidence/t19-live-report.json` endpoint exposure if cited
   publicly** (review 1, follow-up 3). Checked mechanically (pattern grep
   only, file contents not printed): 0 `https?://` occurrences; no
   `endpoint`/`baseUrl`/`url`/`host` JSON keys. No sensitive endpoint data
   present. **Verified; nothing to fix.**
4. **`capabilityFingerprint` scope note** (review 2, observation 1). Added
   a doc comment in `src/backend/live/runner.ts` stating the fingerprint
   covers advertised tool names only, not descriptions/parameter schemas,
   and that this should be stated if reports are compared across backend
   upgrades. **Fixed.**
5. **`scoreCase` fabricated-memory conflation** (review 2, observation 2).
   Added a comment in `scripts/model-eval.mjs` acknowledging that a
   fabricated memory is counted as `forbiddenScopeLeakage` even though it
   is an extraction false positive, and that real leakage gating uses
   planted cross-scope fixtures so the frozen zero-threshold stays
   meaningful. **Fixed.** Threshold semantics unchanged; no rubric edit.

## Honest remaining limits

- The two edits are comments and plan text only; they do not change
  thresholds, fingerprint inputs, or runtime behavior, so frozen gates are
  untouched. A reviewer could still argue the B09 checklist items are
  aspirational until B09 executes — that is inherent to a plan document.
- The B09 checklist item is a process guarantee, not an enforced gate;
  nothing mechanical prevents a B09 commit from forgetting the
  `DEFAULT_RUNNER_VERSION` bump. The in-code comment is the second line of
  defense, not a test.
- The t19-live-report.json check was pattern-based on the current file
  state; a regenerated report would need the same check before public
  citation.
- B09 gates (version bump, independent sign-off, exact-Node re-checks)
  remain open per plan, as both reviews noted. Not resolvable in this task.
- Untracked beta material remains uncommitted per "no commit unless
  assigned".

## Verification actually run

- `npx prettier --check src/backend/live/runner.ts scripts/model-eval.mjs
tasks/beta-release-plan.md`: clean.
- `npm run typecheck`: clean.
- `node --test test/live-runner.test.ts test/model-eval.test.ts
test/tokenizer-eval.test.ts test/tokenizer-provider-compare.test.ts`:
  42 pass / 0 fail.
- `npm run check`: exit 0 (typecheck + prettier + 715 tests).
- `tasks/evidence/t19-budget-report.json` regenerated by `npm run check`
  as timing jitter; reverted again via `git checkout --` (jitter-only
  pattern confirmed in R01).
- `git diff --check`: clean.
